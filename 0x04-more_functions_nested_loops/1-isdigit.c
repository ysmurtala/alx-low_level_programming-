#include "main.h"

/**
 * _isdigit - this checks for digit from 0 to 9
 * @c: function prototype
 * Return: success 1 failure 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 47)
		return (1);
	else
		return (0);
}
